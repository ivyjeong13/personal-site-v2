import { Handjet, Jacquard_24, Micro_5, Pixelify_Sans } from 'next/font/google';

export const micro5 = Micro_5({
  weight: ['400'],
  subsets: ['latin'],
});

export const jacquard24 = Jacquard_24({
  weight: ['400'],
  subsets: ['latin'],
});

export const handjet = Handjet({
  weight: ['400', '500'],
  subsets: ['latin'],
});

export const pixelify = Pixelify_Sans({
  weight: ['400', '500'],
  subsets: ['latin'],
});
